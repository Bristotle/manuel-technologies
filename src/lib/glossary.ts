/* Glossary. Plain English definitions of the terms this site uses, each
   linking to the service or tool where the thing is done. Definitions are
   among the most cited page types in AI answers, because a model needs
   somewhere to point when it explains a term, and they are the internal
   link anchors for every higher intent page.

   RULES. Every entry is written, not generated from a pattern. At least 90
   words in the body, one sentence definition first, British English, and a
   real link to where the term matters on this site. The gate below enforces
   the length and the link so a stub cannot ship. */

export type Term = {
  slug: string;
  term: string;
  short: string;            /* one sentence, the definition an answer engine lifts */
  body: string[];           /* two or three paragraphs */
  related: string[];        /* other slugs */
  where: { label: string; href: string };
  cluster: "search" | "build" | "ai" | "commerce";
  modified: string;
};

const D = "2026-09-21";

export const TERMS: Term[] = [
  { slug: "geo", term: "GEO (generative engine optimisation)", cluster: "search", modified: D,
    short: "GEO is the practice of making a business and its content visible, understood and cited inside AI answer engines such as ChatGPT, Claude, Perplexity and Google's AI Overviews.",
    body: ["Where SEO is about ranking a page in a list of results, GEO is about being the source an answer engine draws on when it writes a reply. The engine has to be able to crawl the site, has to understand what the business is, and has to judge the content worth citing. Each of those is a separate piece of work.", "In practice GEO means allowing AI crawlers in robots.txt, declaring the organisation clearly in structured data, writing answer first content with visible sources, and publishing things worth citing, such as original data. Our study of 56 UK accountancy firms found roughly one in ten blocking AI crawlers without knowing it, which is the first thing GEO fixes."],
    related: ["ai-crawler", "structured-data", "answer-engine"], where: { label: "GEO service", href: "/grow/geo" } },
  { slug: "answer-engine", term: "Answer engine", cluster: "search", modified: D,
    short: "An answer engine is a search system that replies with a written answer drawn from sources, rather than a ranked list of links.",
    body: ["ChatGPT with browsing, Perplexity, Claude with web access and Google's AI Overviews are answer engines. They read pages, decide which are trustworthy for the question asked, and compose a reply, usually with citations. The business that gets cited wins the visit or the recommendation; the rest are not shown at all.", "Being cited depends on being readable by the engine's crawler, being clear about who you are, and saying something the engine can quote. A page written as an answer, with the number or the definition first, is far more likely to be lifted than one that warms up for three paragraphs."],
    related: ["geo", "ai-crawler"], where: { label: "GEO service", href: "/grow/geo" } },
  { slug: "ai-crawler", term: "AI crawler", cluster: "search", modified: D,
    short: "An AI crawler is a bot that reads websites on behalf of an AI system, either to train it or to fetch pages when it answers a question.",
    body: ["The main ones name themselves: GPTBot for OpenAI, ClaudeBot for Anthropic, PerplexityBot, Google-Extended for Google's AI products, and Applebot-Extended. They obey robots.txt, so a single Disallow line removes a site from every answer those systems give.", "Many sites block them without a decision ever being made, because a security plugin or a hosting template shipped the rule. Our free crawler check reads your robots.txt and reports exactly which of these bots are allowed, which is the first thing to know before any other AI visibility work.", "The check takes seconds and the fix is one line, which is why it comes first."],
    related: ["robots-txt", "geo"], where: { label: "AI crawler check", href: "/free-tools/ai-crawler-check" } },
  { slug: "robots-txt", term: "robots.txt", cluster: "search", modified: D,
    short: "robots.txt is a plain text file at the root of a website that tells crawlers which parts of the site they may and may not read.",
    body: ["It lives at yourdomain.com/robots.txt and consists of User-agent lines naming a crawler and Allow or Disallow lines for paths. A well behaved crawler reads it before anything else. It can also declare where the sitemap is.", "It is not security, since anything can ignore it, but every search engine and AI crawler that matters respects it. That makes it powerful in both directions: one wrong line hides a site from Google, and one line for GPTBot removes it from ChatGPT's answers. It should be short, deliberate and checked after every platform change."],
    related: ["ai-crawler", "xml-sitemap", "crawlability"], where: { label: "Free audit", href: "/free-audit" } },
  { slug: "xml-sitemap", term: "XML sitemap", cluster: "search", modified: D,
    short: "An XML sitemap is a machine readable list of a site's pages, with the date each last changed, that search engines use to discover and prioritise crawling.",
    body: ["It does not make pages rank, but it makes sure they are found, and it tells the engine which have changed. On a site with more than a few dozen pages it is the difference between a new page being indexed in days and in months.", "The date matters more than most people think. If lastmod is stamped with the build time on every page, Google learns the signal is meaningless and stops trusting it for the whole site. A sitemap should carry the real date the content changed, and nothing else."],
    related: ["robots-txt", "indexation", "crawlability"], where: { label: "Technical SEO", href: "/grow/technical-seo" } },
  { slug: "crawlability", term: "Crawlability", cluster: "search", modified: D,
    short: "Crawlability is whether a search engine or AI crawler can reach and read a page at all.",
    body: ["A page that cannot be crawled does not exist as far as search is concerned, however good it is. Crawlability fails in quiet ways: a robots.txt rule, a bot protection page served to identified crawlers, a login wall, content that only appears after JavaScript the crawler does not run, or a link graph where the page is never linked from anywhere.", "It is the first thing a technical audit checks because nothing else matters until it passes. Our free audit fetches the page the way a crawler does and reports what it could and could not read."],
    related: ["indexation", "robots-txt", "internal-linking"], where: { label: "Free audit", href: "/free-audit" } },
  { slug: "indexation", term: "Indexation", cluster: "search", modified: D,
    short: "Indexation is a search engine adding a page to its index, the database it answers queries from, after crawling it.",
    body: ["Crawled is not indexed. Google can fetch a page and still decline to index it, because it duplicates another, because a canonical tag points elsewhere, because it carries noindex, or because the engine judged it thin. Search Console reports each of these reasons by URL.", "Indexation problems are the most common cause of a site that seems fine and gets no traffic. The fix is usually one of a handful of things: a consistent canonical, a real sitemap date, more internal links, or more substance on the page.", "Check the Pages report in Search Console before assuming a ranking problem; most are indexation problems wearing a disguise."],
    related: ["canonical-tag", "crawlability", "xml-sitemap"], where: { label: "Technical SEO", href: "/grow/technical-seo" } },
  { slug: "canonical-tag", term: "Canonical tag", cluster: "search", modified: D,
    short: "A canonical tag is a line in a page's HTML that names the one URL a search engine should treat as the official version of that content.",
    body: ["Sites produce duplicates without meaning to: with and without www, with and without a trailing slash, with tracking parameters. The canonical tag resolves them to one address so ranking signals are not split.", "It has to agree with everything else. If the canonical says the apex domain but the site redirects to www, Google sees a contradiction and can refuse to index either. This site once lost a week of indexing to exactly that mismatch, which is why it is now checked in the build.", "Every page should carry one, pointing at itself, on the domain the site actually serves."],
    related: ["indexation", "redirect"], where: { label: "Technical SEO", href: "/grow/technical-seo" } },
  { slug: "structured-data", term: "Structured data (schema markup)", cluster: "search", modified: D,
    short: "Structured data is machine readable information embedded in a page, usually as JSON-LD using schema.org vocabulary, that states plainly what the page is about.",
    body: ["A human sees a heading, a price and a star rating. A machine sees text. Structured data says: this is a Product, its price is this, it is sold by this Organization at this address. Search engines use it for rich results, and answer engines use it to be sure who a business is.", "The common types for a business site are Organization, LocalBusiness, Service, Product, FAQPage, Article and BreadcrumbList. In our benchmarks of Ghanaian industries, between 47% and 63% of sites carried none at all. Our free schema generator produces the main types as you type."],
    related: ["rich-results", "geo", "entity"], where: { label: "Schema generator", href: "/free-tools/schema-generator" } },
  { slug: "rich-results", term: "Rich results", cluster: "search", modified: D,
    short: "Rich results are search listings enhanced with extra elements such as star ratings, prices, FAQs or images, drawn from a page's structured data.",
    body: ["They take more space on the results page and get clicked more than a plain blue link. Google decides whether to show them, but it can only do so when the page carries valid structured data of the right type and the visible content matches it.", "The types most often available to a business site are FAQ, Product, Article, Breadcrumb and Organisation details. A page with no structured data is ineligible for all of them, whatever its ranking.", "Google's Rich Results Test shows in seconds whether a page qualifies, and which fields are missing when it does not."],
    related: ["structured-data"], where: { label: "On-page SEO", href: "/grow/on-page-seo" } },
  { slug: "entity", term: "Entity (in search)", cluster: "search", modified: D,
    short: "An entity is a thing a search engine knows about as a distinct object, such as a company, a person or a place, rather than as a string of words.",
    body: ["Google and AI systems build a graph of entities and the relationships between them. A business that is a recognised entity gets a knowledge panel, gets connected to its founder and its services, and gets recommended by name. One that is only a domain gets none of that.", "Becoming an entity is corroboration: consistent name, address and phone across the site and its profiles, Organization schema with sameAs links to LinkedIn and other verified profiles, and being mentioned by other sources. It is the foundation under GEO.", "The test is simple: search the business name. If Google shows a panel with the right details, the entity exists. If it shows a list of links, it does not yet."],
    related: ["structured-data", "geo", "nap"], where: { label: "GEO service", href: "/grow/geo" } },
  { slug: "nap", term: "NAP (name, address, phone)", cluster: "search", modified: D,
    short: "NAP is a business's name, address and phone number, which must be identical everywhere they appear for local search to trust them.",
    body: ["Google cross references the NAP on your website, your Google Business Profile, directories and social profiles. Variations, such as Ltd on one and Limited on another or two different phone numbers, read as two different businesses and weaken both.", "For a Ghanaian business the practical version is: one phone number formatted one way, one address, one name, on the site footer, the contact page, the Organization schema and every profile. Our Google Business Profile checker tests the listing side of this.", "Our own Organization schema carries the Accra address, the phone number and every social profile for exactly this reason."],
    related: ["entity", "local-seo"], where: { label: "Google Business Profile checker", href: "/free-tools/google-business-profile" } },
  { slug: "local-seo", term: "Local SEO", cluster: "search", modified: D,
    short: "Local SEO is the work of appearing in search results for people near you, or searching for a place, including the map results.",
    body: ["Searches like accountant in Kumasi or clinic near me are answered mostly from the map pack and Google Business Profile listings, not from ordinary pages. Local SEO is the profile, its reviews, consistent NAP, location pages on the site, and LocalBusiness schema.", "For most Ghanaian service businesses it is the highest return search work available, because the competition is thin and the intent is immediate. A business with a complete, active profile and a fast site that names its city outranks a bigger firm that has neither.", "Reviews matter as much as the profile: a facility with forty recent reviews outranks one with four, all else being equal."],
    related: ["nap", "structured-data"], where: { label: "Google Business Profile checker", href: "/free-tools/google-business-profile" } },
  { slug: "programmatic-seo", term: "Programmatic SEO", cluster: "search", modified: D,
    short: "Programmatic SEO is generating many pages from one template and a dataset, so that each page targets a specific search with its own data.",
    body: ["A property site with a page per listing, a directory with a page per city, a benchmark with a page per industry: the structure is written once and the data fills it. Done with real per page data it captures long tail searches no hand written site could cover.", "Done without real data it is thin content at scale, and Google's scaled content policy treats it as spam. The test is whether each page says at least three things a reader could not get from any other page on the site. Our own industry pages are built this way, gated in code so a page without its data cannot be generated."],
    related: ["thin-content", "xml-sitemap"], where: { label: "Programmatic SEO service", href: "/grow/programmatic-seo" } },
  { slug: "thin-content", term: "Thin content", cluster: "search", modified: D,
    short: "Thin content is a page with little or no substance of its own, which search engines decline to index or rank.",
    body: ["It includes near duplicate pages, pages that only swap a place name, doorway pages, and pages that exist to hold a keyword. Google's guidance on scaled content abuse targets exactly this when it is produced in volume.", "The cure is substance per page: original data, a real example, a specific answer. If a page cannot carry three sentences that are true only of it, it should be merged into one that can.", "Our programmatic pages are gated in code for exactly this reason: a page without its own data is not built at all, rather than built thin and hoped for."],
    related: ["programmatic-seo", "indexation"], where: { label: "Programmatic SEO risk index", href: "/free-tools/programmatic-seo-risk" } },
  { slug: "internal-linking", term: "Internal linking", cluster: "search", modified: D,
    short: "Internal linking is the set of links between pages on the same site, which carries both visitors and ranking signals from page to page.",
    body: ["A page with no internal links pointing at it is an orphan: hard to find, rarely crawled, weakly ranked. A page linked from many relevant pages is the opposite. Search engines read the link graph as a map of what the site thinks matters.", "The common failure is a star shape, where every page links to the homepage and nothing links sideways. A cluster, where a pillar page links to every post and every post links to the pillar and two siblings, is what passes authority around. Our audit found exactly this problem on this site before it was fixed."],
    related: ["topic-cluster", "crawlability"], where: { label: "On-page SEO", href: "/grow/on-page-seo" } },
  { slug: "topic-cluster", term: "Topic cluster", cluster: "search", modified: D,
    short: "A topic cluster is a pillar page on a broad subject linked to a set of posts on its sub topics, each linking back, so the group ranks as a whole.",
    body: ["Our pricing page is a pillar; the posts on what a website costs, what maintenance costs and how to compare agencies are its cluster. Each post links up to the pillar and sideways to siblings, and the pillar links to all of them.", "The shape tells search engines the site has depth on the subject rather than one page mentioning it. It is also how a reader moves from a specific question to the decision, which is where the enquiry happens.", "A cluster is also the fastest way to spot gaps: the sub topic with no post is the one competitors will answer instead."],
    related: ["internal-linking", "pillar-page"], where: { label: "Content and branding", href: "/grow/content-branding" } },
  { slug: "pillar-page", term: "Pillar page", cluster: "search", modified: D,
    short: "A pillar page is the single comprehensive page on a subject that a topic cluster's posts all link to.",
    body: ["It covers the subject broadly and hands off to the cluster for depth. On this site the pricing page is the pillar for everything about website cost, and the free tools index is the pillar for the tools.", "A pillar earns its position by being the page a reader would bookmark and the page other sites link to. It should be the best page on its subject on the site, not a table of contents.", "Its job is to convert as well as to rank, so it carries the price, the proof and the next step, not only the overview."],
    related: ["topic-cluster", "internal-linking"], where: { label: "Pricing", href: "/pricing" } },
  { slug: "core-web-vitals", term: "Core Web Vitals", cluster: "build", modified: D,
    short: "Core Web Vitals are Google's three measures of a page's real world experience: loading (LCP), interactivity (INP) and visual stability (CLS).",
    body: ["Largest Contentful Paint should be under 2.5 seconds, Interaction to Next Paint under 200 milliseconds, and Cumulative Layout Shift under 0.1. They are measured from real users' browsers, not a lab, and they are a ranking signal.", "They are also the honest test of a build. A site can look finished and fail all three because images are unsized, scripts block the thread and fonts swap late. Our CWV Drift Monitor extension watches them as you browse, and our builds target 100 on all three.", "Real user data appears in Search Console's Core Web Vitals report about a month after a site has enough traffic, and it is that data, not a lab score, that Google ranks on."],
    related: ["lcp", "cls", "ttfb"], where: { label: "Free audit", href: "/free-audit" } },
  { slug: "lcp", term: "LCP (Largest Contentful Paint)", cluster: "build", modified: D,
    short: "LCP is the time from starting to load a page until its largest visible element, usually a hero image or headline, has rendered.",
    body: ["It is the metric closest to what a person means by the page has loaded. Google wants it under 2.5 seconds for 75% of visits. On a mid range phone on mobile data, most sites we measure miss it.", "The causes are predictable: a slow first byte, a large unoptimised hero image, render blocking scripts and stylesheets, and fonts that hold up text. A build that sets image dimensions, serves WebP or AVIF, and preloads the one image that matters gets there.", "On this site the LCP element on most pages is the headline, which is why it renders in under 1.5 seconds on a throttled phone."],
    related: ["core-web-vitals", "ttfb"], where: { label: "Website development", href: "/build/website-development" } },
  { slug: "cls", term: "CLS (Cumulative Layout Shift)", cluster: "build", modified: D,
    short: "CLS measures how much a page's content moves around while it loads, which is what makes a button jump just as you tap it.",
    body: ["The usual cause is an image, advert or font arriving after the page has laid out, pushing everything below it down. Google wants the score under 0.1; a well built page scores 0.", "The fix is reserving space: every image with explicit width and height, fonts that swap without changing metrics, and no content injected above what is already visible. It is a build discipline rather than an optimisation.", "A layout shift is also the single most common cause of a mis-tap on a phone, which makes it a conversion problem as well as a ranking one."],
    related: ["core-web-vitals", "lcp"], where: { label: "Website development", href: "/build/website-development" } },
  { slug: "ttfb", term: "TTFB (Time to First Byte)", cluster: "build", modified: D,
    short: "TTFB is the time between requesting a page and receiving the first byte of the response, which is the server's share of the wait.",
    body: ["It includes DNS lookup, the TLS handshake, any redirects and the server's own work before it starts sending. Everything else on the page waits behind it, so a slow first byte makes every other metric worse.", "Under 800 milliseconds is Google's threshold; a well hosted static page is under 200. Across our Ghanaian industry benchmarks the median was around a second, and in the UK accountancy sample it was 3.5 seconds. It is the number that most separates a properly hosted site from a cheaply hosted one.", "It is also the metric a business owner can check in seconds: open the page, count the delay before anything appears at all."],
    related: ["core-web-vitals", "lcp", "static-site"], where: { label: "Free audit", href: "/free-audit" } },
  { slug: "static-site", term: "Static site (SSG)", cluster: "build", modified: D,
    short: "A static site is one whose pages are generated in advance as finished HTML files and served from a cache, rather than assembled by a server on each request.",
    body: ["Because there is no database query and no server work per visit, a static page is fast everywhere, cheap to host, and hard to take down. Modern frameworks such as Next.js generate the pages at build time and rebuild when content changes.", "It suits marketing sites, documentation, blogs and catalogues with periodic updates. It does not suit a page that must show live per user data, which is what server rendering or client side fetching is for. This site is static throughout, which is why its first byte is under half a second."],
    related: ["ttfb", "headless-cms", "pwa"], where: { label: "Website development", href: "/build/website-development" } },
  { slug: "headless-cms", term: "Headless CMS", cluster: "build", modified: D,
    short: "A headless CMS stores and edits content separately from the website that displays it, delivering the content through an API.",
    body: ["Traditional systems such as WordPress bundle the editor and the site. A headless setup keeps the editor and lets a fast, modern front end fetch the content, so the site can be static and the editing can be simple.", "It suits businesses that want to edit content themselves without the maintenance burden and speed cost of a plugin heavy platform. It is more to set up and usually the right choice for a site expected to grow.", "Sanity, Contentful and a headless WordPress are the usual choices; the front end can be rebuilt later without touching the content."],
    related: ["static-site", "wordpress"], where: { label: "Website development", href: "/build/website-development" } },
  { slug: "wordpress", term: "WordPress", cluster: "build", modified: D,
    short: "WordPress is the most widely used website platform, an open source content management system that runs a large share of the web.",
    body: ["It is a sound choice for many businesses: a huge plugin ecosystem, editors who already know it, and cheap hosting. It is also the platform most often found slow, insecure and abandoned, because plugins accumulate, updates lapse and nobody is responsible.", "Whether to use it is a question of what the site must do and who will maintain it, and we say so plainly on the WordPress or custom build post. A WordPress site with a care plan is fine. One without is the most common source of the hacked site rebuild."],
    related: ["headless-cms", "static-site", "website-maintenance"], where: { label: "WordPress or custom build", href: "/blog/wordpress-vs-custom-website-ghana" } },
  { slug: "pwa", term: "PWA (Progressive Web App)", cluster: "build", modified: D,
    short: "A PWA is a website built to behave like an app: installable on a phone's home screen, working offline, and able to send notifications, without an app store.",
    body: ["For most businesses it delivers what they wanted from an app at a fraction of the cost, with one codebase and no store approval. Fold, our church management product, is a PWA for exactly that reason: congregations install it from a link.", "A native app is still right when the product needs deep hardware access or heavy offline work. For members, customers and staff who need a tool on their phone, a PWA is usually the correct first build.", "It also works offline, which for a congregation, a field team or a school in an area with patchy signal is the feature that decides adoption."],
    related: ["static-site", "mobile-first"], where: { label: "Mobile applications", href: "/build/mobile-apps" } },
  { slug: "mobile-first", term: "Mobile first", cluster: "build", modified: D,
    short: "Mobile first is designing and building for the phone screen before the desktop, because that is where most visitors are.",
    body: ["In Ghana the phone is the primary device, and often a mid range Android on mobile data. A site designed on a large monitor and shrunk to fit is slow, cramped and hard to tap. One designed at 360 pixels wide and expanded reads properly everywhere.", "Google also indexes the mobile version of a site first. A site whose mobile version is a cut down afterthought is ranked on the afterthought.", "It also decides what gets built: a feature that cannot work on a phone is a feature most visitors will never use."],
    related: ["core-web-vitals", "pwa"], where: { label: "Web design", href: "/build/web-design" } },
  { slug: "api", term: "API (application programming interface)", cluster: "build", modified: D,
    short: "An API is the defined way one piece of software lets another send it requests and receive data, which is how systems talk to each other.",
    body: ["When a website takes a Mobile Money payment, the payment gateway's API is what it calls. When a CRM updates a spreadsheet, an API carries the record. Almost every integration is two APIs and the logic between them.", "A system without an API can only be integrated by fragile means such as scraping or manual export. Choosing tools with good APIs is what makes a business's systems connectable later.", "Every integration we build starts by reading the API documentation of both systems, because what an API cannot do sets the limit of what the integration can."],
    related: ["integration", "webhook", "payment-gateway"], where: { label: "Systems and integrations", href: "/build/systems-integrations" } },
  { slug: "webhook", term: "Webhook", cluster: "build", modified: D,
    short: "A webhook is a message one system sends to another the moment something happens, instead of waiting to be asked.",
    body: ["When a payment succeeds, the gateway calls your webhook with the details. When a form is submitted, a webhook can push it into the CRM. It is the mechanism behind most real time automation.", "Webhooks must be verified, since anyone can send a request to a public address, and they must be handled idempotently, since the same event can arrive twice. Getting those two things right is most of the difference between an integration that works and one that double counts orders.", "Every payment integration we build confirms the order from the verified webhook, never from the browser, because the browser can be closed before the payment completes."],
    related: ["api", "integration", "automation"], where: { label: "Systems and integrations", href: "/build/systems-integrations" } },
  { slug: "integration", term: "Systems integration", cluster: "build", modified: D,
    short: "Systems integration is connecting separate software systems so data moves between them automatically and consistently.",
    body: ["The CRM, the accounts package, the online store, the SMS provider and the spreadsheet each hold a version of the customer. Integration makes one of them the source of truth and keeps the others in step, so nobody types anything twice and the numbers agree.", "The hard parts are field ownership, stable identifiers, retries and reconciliation, not the connection itself. An integration built without them works on the demo and drifts in production.", "Our integrations page lists the 52 systems we build against, from payment gateways and CRMs to accounting packages and messaging tools."],
    related: ["api", "webhook", "automation"], where: { label: "Systems and integrations", href: "/build/systems-integrations" } },
  { slug: "automation", term: "Workflow automation", cluster: "ai", modified: D,
    short: "Workflow automation is software carrying out a repeatable business process, such as routing an enquiry or issuing an invoice, without a person doing each step.",
    body: ["A fixed automation follows the same steps every time and is right for most business processes: predictable, testable, cheap. It is what tools like n8n, Make and Zapier do, and what custom code does when those tools run out.", "AI enters when a step needs judgement, such as reading a document or classifying a message. Even then the automation around the model, with its validation and approval points, does most of the work. Our automation ROI calculator puts a number on what a process costs to leave manual.", "A good first automation is one that is reversible, high volume and boring, because it proves the approach without putting anything at risk."],
    related: ["ai-agent", "webhook", "human-in-the-loop"], where: { label: "AI automation", href: "/scale/ai-automations" } },
  { slug: "ai-agent", term: "AI agent", cluster: "ai", modified: D,
    short: "An AI agent is a system in which a language model decides which tools to use and in what order to reach a goal, rather than following a fixed script.",
    body: ["Given a task, an agent can read a situation, call a tool such as a database or an email API, inspect the result, and decide what to do next, including asking a person. That flexibility is the point and the risk.", "Most businesses that think they need an agent need a fixed automation. An agent is right for a narrow set of multi step problems, and only when built with typed tools, limits on what it can touch, evaluation cases and a human approval point for anything hard to reverse.", "When an agent is right, it is right with a narrow scope, and it is measured on the same tasks every week so drift is caught early."],
    related: ["automation", "human-in-the-loop", "llm"], where: { label: "AI agents", href: "/scale/ai-agents" } },
  { slug: "llm", term: "LLM (large language model)", cluster: "ai", modified: D,
    short: "An LLM is a model trained on very large amounts of text that can read, write, summarise, classify and reason over language, the technology behind ChatGPT and Claude.",
    body: ["For a business it is a component, not a product. It is good at text and document work at volume, at drafting, extracting and classifying, and poor at arithmetic, at facts it was not given, and at knowing when it is wrong.", "Used well, with the right context supplied and its output checked, it removes hours of routine work a day. Used as an oracle it invents. The engineering around it, retrieval, validation and review, is where the value is made or lost.", "The models this site's tools use are Anthropic's Claude family, chosen for reliability on long structured tasks."],
    related: ["ai-agent", "rag", "prompt-injection"], where: { label: "AI automation", href: "/scale/ai-automations" } },
  { slug: "rag", term: "RAG (retrieval augmented generation)", cluster: "ai", modified: D,
    short: "RAG is the technique of fetching relevant documents from your own data and giving them to a language model alongside the question, so its answer is grounded in your facts.",
    body: ["A model on its own knows nothing about your price list, your policies or last week's orders. RAG looks those up first, from a search index over your documents, and puts them in front of the model. The answer then cites what it was given.", "It is how a support assistant answers from your actual documentation rather than inventing. Its quality depends almost entirely on the retrieval: what gets indexed, how it is chunked, and whether the right passage is found.", "A RAG system is only as current as its index, so the ingestion of new documents has to be automated or the assistant slowly starts answering from last year."],
    related: ["llm", "ai-agent"], where: { label: "AI agents", href: "/scale/ai-agents" } },
  { slug: "human-in-the-loop", term: "Human in the loop", cluster: "ai", modified: D,
    short: "Human in the loop is designing an automated or AI system so that a person reviews or approves certain outputs before they take effect.",
    body: ["Not every step needs it. A classification that files an email can run unattended. A refund, a contract clause or a message to a customer should wait for a person until the system has earned trust through measured accuracy.", "The approval point is a design decision, not a fallback. It is what lets a business adopt AI on real work without handing it the authority to do damage.", "The approval queue is also where the training data for improving the system comes from: every correction a person makes is a labelled example."],
    related: ["ai-agent", "automation"], where: { label: "AI automation", href: "/scale/ai-automations" } },
  { slug: "prompt-injection", term: "Prompt injection", cluster: "ai", modified: D,
    short: "Prompt injection is an attack in which text fed to a language model, such as a web page or an email, contains instructions the model then follows as if they came from its operator.",
    body: ["An agent that reads customer emails can be told, inside an email, to forward the inbox somewhere. An assistant that browses the web can be steered by a page. The model cannot reliably tell instructions from data.", "The defence is structural: limit what tools the model can call, never give it authority it does not need, validate outputs, and put a person in the loop for anything consequential. It is the reason bounded agents are the only kind worth deploying.", "Treat every piece of text a model reads from outside as data, never as instructions, and design the system so that following a bad instruction cannot cause harm."],
    related: ["ai-agent", "llm"], where: { label: "AI agents", href: "/scale/ai-agents" } },
  { slug: "payment-gateway", term: "Payment gateway", cluster: "commerce", modified: D,
    short: "A payment gateway is the service that takes a payment on a website, handles the card or Mobile Money transaction, and settles the money to the business.",
    body: ["In Ghana the relevant gateways are Paystack, Hubtel and Flutterwave, each supporting MTN MoMo, Telecel Cash and cards, with fees per transaction and settlement times that differ. Choosing one is a fee and a workflow decision.", "Integration is more than a button: it is handling failure, confirming through a verified webhook, and reconciling settlements against orders. A store that gets that wrong loses money quietly, which is why the online store tier on our pricing page includes it.", "For a store selling to the diaspora, card acceptance and multi currency settlement narrow the choice further, and the gateway's fees on international cards are usually higher."],
    related: ["mobile-money", "api", "webhook"], where: { label: "Gateways compared", href: "/payment-gateways-ghana" } },
  { slug: "mobile-money", term: "Mobile Money (MoMo)", cluster: "commerce", modified: D,
    short: "Mobile Money is payment from a balance held against a phone number, which in Ghana means MTN MoMo and Telecel Cash, and is the most common way people pay online.",
    body: ["A Ghanaian online store without Mobile Money turns away most of its customers. Accepting it on a website goes through a gateway, which prompts the customer's phone for approval and reports the result to the site.", "Fees are charged by the gateway and the network, and settlement can take a day. Our guide to accepting MoMo on a website covers the options, the fees and what the integration involves.", "For a service business, MoMo on the website also means deposits can be taken before a job, which removes the most common cause of no shows."],
    related: ["payment-gateway", "ecommerce"], where: { label: "Accepting MoMo on a website", href: "/blog/momo-payment-integration-website-ghana" } },
  { slug: "ecommerce", term: "Ecommerce website", cluster: "commerce", modified: D,
    short: "An ecommerce website is one that sells directly: a catalogue, a cart, a checkout that takes payment, and the order handling behind it.",
    body: ["The catalogue and the cart are the visible part. The expensive part is what happens when payment fails, when stock runs out mid order, when a delivery zone is not served, and when an order needs a refund. A store is not a brochure with a buy button.", "That is why an online store is priced above a business site on our pricing page, and why a competitor's online store priced the same as their brochure site is a brochure with a buy button.", "The store tier on our pricing page includes catalogue, variants, stock, MoMo and card payment, failure handling, order alerts and admin training, because those are what make it a store."],
    related: ["payment-gateway", "mobile-money", "product-schema"], where: { label: "Ecommerce website price in Ghana", href: "/blog/ecommerce-website-price-ghana" } },
  { slug: "product-schema", term: "Product schema", cluster: "commerce", modified: D,
    short: "Product schema is structured data on a product page stating the name, price, currency, availability and rating, so search engines can show the product as a rich result.",
    body: ["With it, a listing can appear in Google with its price and stock status, and in Google Shopping's free listings. Without it, the product is a page like any other.", "For a Ghanaian store the currency must be GHS and the availability must be kept current, since a rich result showing an out of stock item as in stock is worse than none. Our online store builds generate it from the product record.", "The same record that renders the page produces the schema, so the two cannot disagree, which is the failure mode when schema is added by hand."],
    related: ["structured-data", "ecommerce", "rich-results"], where: { label: "Schema generator", href: "/free-tools/schema-generator" } },
  { slug: "website-maintenance", term: "Website maintenance", cluster: "build", modified: D,
    short: "Website maintenance is the ongoing work of keeping a site secure, updated, backed up, fast and online after it launches.",
    body: ["Software gets patched, certificates renew, plugins update, backups run, forms stop sending and somebody has to notice. A site with nobody doing this decays, and a hacked site is the usual end of the story.", "It is why every tier on our pricing page pairs with a care plan, and why the plans are priced on what the work actually costs rather than the GHS 1,500 a month some guides recommend. Our maintenance cost post sets out what should be in it.", "The difference between a site that is maintained and one that is not shows up in the benchmarks: expired certificates, broken forms and last year's calendar are all maintenance failures."],
    related: ["wordpress", "static-site"], where: { label: "Website maintenance cost in Ghana", href: "/blog/website-maintenance-cost-ghana" } },
  { slug: "redirect", term: "Redirect (301 and 308)", cluster: "search", modified: D,
    short: "A redirect is a server response that sends a visitor and a search engine from one URL to another, permanently (301 or 308) or temporarily (302 or 307).",
    body: ["Permanent redirects carry ranking signals to the new address; temporary ones do not. When a site is rebuilt, every old URL that had traffic or links needs a permanent redirect to its replacement, or the traffic is lost.", "Redirects also have to agree with canonical tags. A site that redirects to www while declaring the apex as canonical is telling Google two different things, and Google resolves the contradiction by indexing neither.", "A redirect map is written before a rebuild launches, with every old URL that had traffic or links in it, and it is tested on launch day, not after."],
    related: ["canonical-tag", "indexation"], where: { label: "Technical SEO", href: "/grow/technical-seo" } },
];

/* Gate: no stub can ship. */
for (const t of TERMS) {
  const words = t.body.join(" ").split(/\s+/).length;
  if (words < 90) throw new Error(`glossary: "${t.slug}" has ${words} words, minimum 90`);
  if (!t.where.href.startsWith("/")) throw new Error(`glossary: "${t.slug}" needs an internal link`);
  for (const r of t.related) if (!TERMS.some((x) => x.slug === r)) throw new Error(`glossary: "${t.slug}" relates to unknown "${r}"`);
}

export const getTerm = (slug: string) => TERMS.find((t) => t.slug === slug);
export const CLUSTER_NAMES = { search: "Search and AI visibility", build: "Websites and software", ai: "AI and automation", commerce: "Payments and ecommerce" } as const;
